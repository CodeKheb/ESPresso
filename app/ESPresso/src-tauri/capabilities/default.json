{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "sql:default",
    "sql:allow-execute",
    "sql:allow-select",
    "core:default",
    "opener:default"
  ]
}
